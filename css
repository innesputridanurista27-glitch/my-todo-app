/* Gaya untuk tugas yang terlambat */
        .task-card.late-task {
            background-color: #fff5f5;
            border-right: 4px solid #e74c3c;
        }

        .late-label {
            background: #e74c3c;
            color: white;
            font-size: 9px;
            padding: 2px 6px;
            border-radius: 4px;
            font-weight: bold;
            display: inline-block;
            margin-bottom: 5px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0% { opacity: 1; }
            50% { opacity: 0.6; }
            100% { opacity: 1; }
        }

        @keyframes slideOut {
    to { transform: translateX(100%); opacity: 0; }
}
.removing {
    animation: slideOut 0.3s ease forwards;
}

/* Gaya untuk Tampilan Kosong (Empty State) */
.empty-state {
    text-align: center;
    padding: 60px 20px;
    color: #bdc3c7;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 15px;
}

.empty-state .icon {
    font-size: 64px;
    margin-bottom: 10px;
    filter: grayscale(1);
    opacity: 0.5;
    animation: float 3s ease-in-out infinite;
}

.empty-state p {
    font-size: 16px;
    font-weight: 500;
    color: #7f8c8d;
}

.empty-state .sub-text {
    font-size: 13px;
    color: #95a5a6;
}

@keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
}
/* Header Background diperlebar */
.profile-header { 
    width: 100%; 
    height: 200px; /* Ubah dari 140px ke 200px atau sesuai keinginan */
    background: #ddd; 
    background-size: cover; 
    background-position: center; 
    position: relative; 
    border-bottom: 2px solid white; 
}

/* Penyesuaian posisi Avatar agar tetap seimbang */
.avatar-wrapper { 
    position: relative; 
    width: 120px; /* Sedikit diperbesar agar kontras dengan header lebar */
    height: 120px; 
    margin: -60px auto 10px; /* Margin minus setengah dari tinggi avatar */
    z-index: 5; 
}
/* Header Background dengan Gradasi */
.profile-header { 
    width: 100%; 
    height: 220px; 
    background: #ced4da; 
    background-size: cover; 
    background-position: center; 
    position: relative; 
}

/* Overlay Gradasi agar tombol tetap terlihat jelas */
.profile-header::after {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: linear-gradient(to bottom, rgba(0,0,0,0) 60%, rgba(0,0,0,0.4) 100%);
    pointer-events: none; /* Agar gradasi tidak menghalangi klik tombol */
}
/* Style Footer Copyright */
.footer-copyright {
    font-family: 'Roboto', sans-serif;
    text-align: center;
    padding: 20px 10px;
    margin-top: 30px;
    font-size: 12px;
    letter-spacing: 0.5px;

    /* Warna akan mengikuti variabel teks utama halaman, 
       otomatis berubah saat mode gelap/terang */
    color: var(--text-color); 
    opacity: 0.7;
    width: 100%;
    border-top: 1px solid var(--border-color, rgba(0,0,0,0.05));
}

/* Penyesuaian khusus jika variabel berbeda di beberapa halaman */
body.dark-mode .footer-copyright {
    color: var(--text-main, #e0e0e0);
    border-top: 1px solid rgba(255,255,255,0.1);
}